{% extends 'base.html.twig' %}

{% block title %}Edit Book{% endblock %}

{% block body %}
    <h1>Edit Book</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button>{{ button_label|default('Update') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('library_home') }}">Back</a>
{% endblock %}
